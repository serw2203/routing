'use strict';

import * as React from 'react';
import * as ReactDOM from 'react-dom';

const APP_CONTAINER_ID = 'routing';

ReactDOM.render(
    <h1>Hello, world !!!</h1>,
    document.getElementById(APP_CONTAINER_ID)
);